# rat_quickdb 已有能力索引

本文档记录项目已有的模块、工具函数、设计模式及其位置，便于实现新功能时查找已有实现，避免重复造轮子。

## 核心入口

| 能力 | 位置 | 说明 |
|------|------|------|
| `init()` | `src/lib.rs` | 初始化 i18n 系统，必须在使用前调用 |
| `generate_object_id()` | `src/lib.rs` | 生成 MongoDB 风格的 ObjectId |
| `process_data_fields_from_metadata()` | `src/lib.rs` | 处理数据库返回的原始数据（JSON/Bool/DateTime 转换） |
| `lock_global_operations()` | `src/lib.rs` | 锁定全局操作，禁止添加新数据库配置 |
| `is_global_operations_locked()` | `src/lib.rs` | 检查全局操作是否已锁定 |
| `get_info()` | `src/lib.rs` | 获取库版本和名称信息 |

## 重新导出类型

从 `src/lib.rs` 重新导出的常用类型：

```rust
// 错误处理
pub use error::{QuickDbError, QuickDbResult};

// 管理器函数
pub use manager::{
    add_database, drop_table, get_aliases, health_check, register_model, set_default_alias,
    table_exists, clear_all_caches, clear_cache, get_cache_manager, get_cache_stats,
};

// 连接
pub use pool::DatabaseConnection;

// 类型系统
pub use types::*;

// 模型相关
pub use model::{
    FieldDefinition, FieldType, IndexDefinition, Model, ModelManager, ModelMeta, ModelOperations,
    array_field, bigint_field, boolean_field, datetime_field, datetime_with_tz_field, dict_field,
    float_field, integer_field, json_field, list_field, reference_field, string_field, uuid_field,
};

// ODM 相关
pub use odm::{AsyncOdmManager, OdmOperations, get_odm_manager, get_odm_manager_mut};

// 适配器
pub use adapter::{DatabaseAdapter, create_adapter};

// 配置
pub use config::{
    AppConfig, AppConfigBuilder, DatabaseConfigBuilder, Environment, GlobalConfig,
    GlobalConfigBuilder, LogLevel, LoggingConfig, LoggingConfigBuilder, PoolConfigBuilder,
    mongodb_config, mysql_config, postgres_config, sqlite_config,
};

// 表管理
pub use table::{ColumnDefinition, ColumnType, IndexType, TableManager, TableSchema};

// 缓存
pub use cache::{CacheManager, CacheStats};

// ID 生成器
pub use id_generator::{IdGenerator, MongoAutoIncrementGenerator};

// 存储过程
pub use stored_procedure::*;

// 字段版本管理
pub use field_versioning::{FieldVersionManager, ModelVersionMeta, VersionChange, VersionUpgradeResult};
```

## 详细文档索引

各模块的详细文档请参考：

- **模型层**：[model.md](model.md) - Model trait、define_model! 宏、ModelManager、字段类型、数据转换
- **ODM 层**：[odm.md](odm.md) - AsyncOdmManager、OdmOperations trait、Handler 模式、全局函数
- **适配器层**：[adapter.md](adapter.md) - DatabaseAdapter trait、各数据库实现、缓存装饰器
- **类型系统**：[types.md](types.md) - DataValue、QueryCondition、UpdateOperation、IdStrategy、DatabaseConfig
- **缓存系统**：[cache.md](cache.md) - CacheManager、缓存键生成、查询缓存、记录缓存
- **连接池**：[pool.md](pool.md) - MultiConnectionManager、SqliteWorker、ConnectionPool
- **配置系统**：[config.md](config.md) - AppConfig、ConfigBuilder、环境配置、日志配置
- **其他模块**：[other.md](other.md) - 存储过程、字段版本管理、表管理、工具函数、错误处理、安全、序列化、日志、i18n
